/**
 * Created by Dave on 11/18/13.
 */
var gameScenes = function(parent){
    //alias parent, so we know what we're dealing with
    var app = parent;

    /*
    var scene1 = require('assets/scenes/scene1');
    app.scene1 = scene1(app);

    etc..
     */
}

module.exports = gameScenes;